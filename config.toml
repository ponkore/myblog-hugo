baseURL = "http://ponkore.github.io/"
languageCode = "ja-jp"
title = "(scratchpad memories)"
paginate = 5
theme = "mainroad"
disqusshortname = "ponkore" # Enable comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id


[Author]
  name = "ponkore"
  bio = "xxx"
  avatar = ""

# [permalinks]
#   post = "/:year/:month/:title/"
#   page = "/:slug/"

#
# Theme variables
#
[Params]
  subtitle = "subtitle"
  description = "主に自分用メモの小ネタを書いていきます。"
  opengraph = true
  readmore = false
  leftsidebar = false
  authorbox = false
  postnavigation = true
  postSections = ["post"] # the section pages to show on home page and the "Recent articles" widget
  #postSections = ["blog", "news"] # alternative that shows more than one section's pages
  #dateformat = "2006-01-02" # change the format of dates
  # date_format = "Mon Jan 02, 2006"
  date_format = "2006/01/02"

[Params.widgets]
    search = true # Enable "Search" widget
    recent_articles = true # Enable "Recent articles" widget
    recent_articles_num = 10 # Set the number of articles in the "Recent articles" widget
    categories = true # Enable "Categories" widget
    tags = true # Enable "Tags" widget
    tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

    # Enable "Social" widget, if any of "social_*" set a value
    social_facebook = "masa0.kato"
    social_twitter = "ponkore"
    # social_linkedin = "username"
    social_github = "ponkore"
    # social_email = "example@example.com"

# vim: ts=2 sw=2 et
